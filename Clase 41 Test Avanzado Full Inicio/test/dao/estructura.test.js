/*
import mongoose from "mongoose";
import Assert from "assert";
import Users from "../../src/dao/Users.dao.js";

mongoose.connect(
  "mongodb+srv://CoderUser:A123456*@pruebacoder.rpvqwdz.mongodb.net/?retryWrites=true&w=majority"
);

const assert = Assert.strict;
// lleva una descripcion y una funcion y la funcion es un contexto

describe("titulo del grupo de pruebas User DAO test cases", () => {

  //para definir las pruebas es con el it recibe los mismos parametros

  before(() => {
    console.log("una vez antes de cada prueba");
  });

  beforeEach(() => {
    console.log("antes de cada prueba");
  });

  afterEach(() => {
    console.log("despues de cada prueba");
  });
  after(() => {
    console.log(" despues de cada prueba");
  });

  it('Prueba 1',()=>{
    assert.strictEqual(true,true)
  })

  it('Prueba 2',()=>{
    assert.strictEqual(true,false)
  })


});*/
